const API=(window.APEXGRID_API_URL||localStorage.getItem('apexgrid_api_url')||'http://localhost:8000').replace(/\/$/,'');const S={world:{},cars:[],events:[],selected:null};const $=id=>document.getElementById(id);const ctx=$('map').getContext('2d');async function get(p){const r=await fetch(API+p);if(!r.ok)throw Error();return r.json()}function render(){const w=S.world||{};$('title').textContent=w.title||'APEXGRID World';$('msg').textContent=w.message||'Persistent AI motorsport simulation';$('season').textContent=w.season||'—';$('round').textContent=w.round??'—';$('track').textContent=w.track||'Track';$('lap').textContent=w.lap_total?`LAP ${w.lap||0} / ${w.lap_total}`:(w.state||'WAITING');$('world').innerHTML=[['State',w.state],['Weather',w.weather],['Track wetness',(w.track_wetness??0)+'%']].map(x=>`<div class="row"><span>${x[0]}</span><b>${x[1]||'—'}</b></div>`).join('');$('cars').innerHTML=S.cars.map((c,i)=>`<div class="car" data-id="${c.driver_id}"><span class="pos">P${i+1}</span><span class="name">${c.driver_name}<small class="team">${c.team}</small></span><span>${c.gap||'—'}</span></div>`).join('')||'<div class="selected">No live car telemetry yet.</div>';$('events').innerHTML=S.events.slice(-10).reverse().map(e=>`<div class="event"><b>${e.kind||'EVENT'}</b><br>${e.text||''}</div>`).join('')||'<div class="event">No events yet.</div>';document.querySelectorAll('.car').forEach(e=>e.onclick=()=>{const c=S.cars.find(x=>x.driver_id===e.dataset.id);if(c)$('selected').innerHTML=`<b>${c.driver_name}</b><br>Position ${c.position||'—'} · Speed ${c.speed||'—'} km/h<br>Tyres ${c.tyres||'—'} · Fuel ${c.fuel??'—'}% · Energy ${c.energy??'—'}%`})}function draw(){ctx.clearRect(0,0,900,520);ctx.fillStyle='#090d12';ctx.fillRect(0,0,900,520);ctx.strokeStyle='#252e3b';ctx.lineWidth=44;ctx.beginPath();ctx.moveTo(120,380);ctx.bezierCurveTo(80,120,250,70,440,150);ctx.bezierCurveTo(650,240,790,110,790,330);ctx.bezierCurveTo(790,470,620,470,470,390);ctx.bezierCurveTo(300,300,160,500,120,380);ctx.stroke();ctx.strokeStyle='#768090';ctx.lineWidth=2;ctx.setLineDash([8,8]);ctx.stroke();ctx.setLineDash([]);S.cars.forEach((c,i)=>{const a=(performance.now()/1000*(.12+(i%5)*.004)+i*.31)%(Math.PI*2);ctx.beginPath();ctx.fillStyle=c.color||'#fff';ctx.arc(450+300*Math.cos(a),280+160*Math.sin(a),6,0,7);ctx.fill()});requestAnimationFrame(draw)}async function refresh(){try{const [w,h]=await Promise.all([get('/world'),get('/history')]);S.world=w;S.cars=w.cars||[];S.events=h.events||[];$('status').textContent='ONLINE';render()}catch(e){$('status').textContent='OFFLINE'}}function ws(){try{const s=new WebSocket(API.replace(/^http/,'ws')+'/ws/live');s.onopen=()=>$('status').textContent='LIVE';s.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='world'){S.world=m.data;S.cars=m.data.cars||S.cars}if(m.type==='event')S.events.push(m.data);render()};s.onclose=()=>setTimeout(ws,3000)}catch(e){setTimeout(ws,3000)}}draw();refresh();ws();setInterval(refresh,10000);